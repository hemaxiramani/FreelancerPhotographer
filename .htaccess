<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # If the request is not for an existing file or directory, send to public
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
